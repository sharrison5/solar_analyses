name: solar-analyses

channels:
  - conda-forge
  - defaults

dependencies:
  - python = 3
  - ipython
  - matplotlib
  - numpy
  - pandas
  - pip
  - pre-commit
  - scipy
  - tabulate
  - pip:
    - pystan
    - ruff = 0.7.3
